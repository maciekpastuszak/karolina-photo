<% layout('layouts/boilerplate')%>
    <div class="container pt-3">
        <div class="d-flex justify-content-between">
            <div></div>
            <div>
                <h2 class="pshoot-header">Sesje dziecięce</h2>
            </div>
           
            <div>
                <% if(currentUser) { %>
                    <a class="btn btn-warning" href="/dzieci/new">Dodaj zdjęcie</a>
                    <% } %>
            </div>
        </div>
        <div class="fade_rule"></div>
        <br>
        <p class="card-text">Sesja dziecięca – o co warto zadbać przed rozpoczęciem zdjęć? Oczywiście o dobry nastrój małego modela! Maluszek powinien być wypoczęty i nakarmiony, tak aby czerpał radość z sesji i dobrze się bawił. Ja zadbam o to aby atmosfera podczas sesji była luźna i przyjazna, dobiorę rekwizyty które zaciekawią dziecko.</p>
    
        <div class="container">
            <div class="row justify-content-start">
                <% for (let kid of kids) { %>
                    <div class="col-xs-12 col-md-6 px-2 py-2">
                        <div class="pshoot-card card mb-3">
                            <div class="card-body p-0">
                                <% if(currentUser) { %>
                                <a href="dzieci/<%=kid._id%>">link</a>
                                <% } %>
                                <div>
                                    <% if(kid.images.length) {%>
                                        <img class="card-img-top" src="<%= kid.images[0].url %>" alt="card-img"
                                            id="pshoot-img">
                                        <% } else {%>
                                            <img class="card-img-top"
                                                src="https://res.cloudinary.com/dqcadja0y/image/upload/v1617459271/sample.jpg"
                                                alt="card-img" id="pshoot-img">
                                            <% } %>
                                               
                                </div>
                                    
                            </div>
                        </div>
                    </div>
                    <% } %>
            </div>
        </div>
    </div>